{
  "devDependencies": {
    "lerna": "2.0.0-beta.38"
  },
  "scripts": {
    "server:start": "cd packages/server && npm run start",
    "client:start": "cd packages/client && npm run start",
    "client:build": "lerna run build --stream",
    "client:serve": "serve --single packages/client/build",
    "test": "lerna run test --stream"
  },
  "dependencies": {
    "serve": "^5.1.2"
  }
}
